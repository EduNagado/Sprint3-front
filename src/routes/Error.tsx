import { MainError } from "../styled"

export default function Error(){

    return(
        <MainError>
            <h1>Página não encontrada. Tente novamente mais tarde.</h1>
        </MainError>
    )
}